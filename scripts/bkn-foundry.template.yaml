# Product composition TEMPLATE for gen-dev-manifest.sh — NOT a release lockfile.
# Lists the releases that make up bkn-foundry; the version fields below are
# placeholders the generator rewrites (newest main build per chart, or a
# branch build with --branch). Release lockfiles live in release-manifests/.
apiVersion: deploy.openbkn.ai/v1alpha1
kind: VersionSet
product: bkn-foundry
version: main
source:
  type: oci
  registry: oci://ghcr.io/openbkn-ai/charts
releases:
  core-data-migrator:
    chart: core-data-migrator
    version: main
    stage: pre
  bkn-safe:
    chart: bkn-safe
    version: main
  mf-model-manager:
    chart: mf-model-manager
    version: main
  mf-model-api:
    chart: mf-model-api
    version: main
  kafka-connect:
    chart: kafka-connect
    version: main
  # Installed before every Evidence producer. The producers deliver trace
  # envelopes to agent-observability, and each release relaxes the owner
  # contract in lockstep: a producer that ships ahead of agent-observability
  # sends an envelope the older consumer still rejects, which parks the outbox
  # in retry and blocks the stream head until the consumer catches up. Consumer
  # first is the only ordering that has no such window.
  agent-observability:
    chart: agent-observability
    version: main
  vega-backend:
    chart: vega-backend
    version: main
  bkn-backend:
    chart: bkn-backend
    version: main
  ontology-query:
    chart: ontology-query
    version: main
  agent-operator-integration:
    chart: agent-operator-integration
    version: main
  oss-gateway-backend:
    chart: oss-gateway-backend
    version: main
  sandbox:
    chart: sandbox
    version: main
  agent-retrieval:
    chart: agent-retrieval
    version: main
  bkn-agent:
    chart: bkn-agent
    version: main
  otelcol-contrib:
    chart: otelcol-contrib
    version: main
  bkn-studio:
    chart: bkn-studio
    version: main
